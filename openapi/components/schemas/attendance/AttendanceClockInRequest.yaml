AttendanceClockInRequest:
  type: object
  required:
    - workDate
    - startTime
  properties:
    workDate:
      type: string
      format: date
    startTime:
      type: string
      format: date-time