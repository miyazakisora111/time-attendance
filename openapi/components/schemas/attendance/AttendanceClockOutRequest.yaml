# 退勤打刻HTTPリクエスト。
AttendanceClockOutRequest:
  type: object
  required:
    - workDate
    - endTime
  properties:
    workDate:
      type: string
      format: date
      description: 勤務日（YYYY-MM-DD）
    endTime:
      type: string
      format: date-time
      description: 退勤日時（ISO 8601）