# ダッシュボードからの打刻リクエスト。
# action は ClockAction enum（in / out / breakStart / breakEnd）を指定する。
# レスポンスは更新後の DashboardResponse 全体を返す。
DashboardClockRequest:
  type: object
  required:
    - action
  properties:
    action:
      $ref: "../../enums/ClockAction.yaml#/ClockAction"
      description: 打刻アクション種別
