import type { Transition } from 'framer-motion';

export const transitionFast: Transition = { duration: 0.15, ease: 'easeOut' };
export const transitionNormal: Transition = { duration: 0.25, ease: 'easeOut' };
export const transitionSlow: Transition = { duration: 0.4, ease: 'easeOut' };
